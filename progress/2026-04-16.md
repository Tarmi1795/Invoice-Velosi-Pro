# Progress Summary — 2026-04-16

---

## Fixed

- **Invoice pages JSON parse error** — `/invoices/proforma` and `/invoices/final` were throwing `Unexpected token '<'` error. API path `/api/workflow-presets` was wrong, corrected to `/api/presets`

- **ITP/PO monitoring table missing columns** — `PO NO` and `INSPECTOR` columns were present in API but not displayed in the table. Added both columns.

---

## Added

- **ITP no field on Inspections page** — Added `itp_po_id` dropdown that links each inspection to an ITP/PO record. Also added `itp_no` column to the inspections table.

- **employee_no field on Inspectors page** — Added to:
  - UI form input
  - Table column (`EMPLOYEE NO`)
  - Batch upload expected headers
  - AI column mapper

- **AI batch upload field recognition** — Added to `itp_pos` ENTITY_FIELDS in `aiColumnMapper.ts`:
  - `project_name`
  - `inspector_name`
  - `po_no`
  - `employee_no`
  - Also added `visit_ref` and `itp_po_id` to `inspections` ENTITY_FIELDS

---

## Known Issue (Pending Fix)

- **ITP/PO batch upload fails** — Error: `Unknown argument project_name`. The FK lookup fields (`project_name`, `inspector_name`, `employee_no`) are being passed to Prisma's `itp_pos.create()` but are not actual DB columns — they must be stripped before the create call. Fix drafted in plan.

---

## Commits Today

```
c185fa8 feat: add employee_no to inspectors and ITP/PO batch upload fields
5153bdf fix: add po_no and inspector_name to ITP/PO monitoring table
2e861ed feat: add ITP no field to inspections, linked to ITP/PO reference
60d97dc fix: correct workflow-presets API path to presets in Final Invoice page
```
